(function(){
function chartDays(){return Object.keys(S.days).filter(d=>S.days[d]?.foods?.length||S.days[d]?.burns?.length||S.days[d]?.weight!=null).sort()}
function fmtK(v){return Math.round(v).toLocaleString('en-IN')}
function makeChart(data,target,unit){if(data.length<1)return `<div class="chartEmpty"><b>No data yet</b><span>Log your food and weight to build the chart.</span></div>`;const W=700,H=280,L=48,R=18,T=22,B=42;let vals=data.flatMap(p=>[p.theory,...(p.actual==null?[]:[p.actual])]);if(target!=null)vals.push(target);let min=unit==='kcal'?Math.min(0,...vals):Math.min(...vals),max=unit==='kcal'?Math.max(target||0,...vals):Math.max(...vals);if(min===max){min-=1;max+=1}const X=i=>L+(W-L-R)*(i/Math.max(data.length-1,1)),Y=v=>T+(H-T-B)*(max-v)/(max-min);let grid='';for(let i=0;i<5;i++){let y=T+i*(H-T-B)/4,v=max-(max-min)*i/4;grid+=`<line x1="${L}" y1="${y}" x2="${W-R}" y2="${y}"/><text x="5" y="${y+4}">${unit==='kg'?v.toFixed(1):fmtK(v)}</text>`}function line(key,stroke){let pts=data.map((p,i)=>p[key]==null?null:[X(i),Y(p[key])]).filter(Boolean);if(pts.length<2)return '';return `<path d="${pts.map((p,i)=>(i?'L':'M')+p[0].toFixed(1)+' '+p[1].toFixed(1)).join(' ')}" fill="none" stroke="${stroke}" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>`}function dots(key,fill){return data.map((p,i)=>p[key]==null?'':`<circle cx="${X(i)}" cy="${Y(p[key])}" r="4" fill="${fill}"/>`).join('')}let goal=target!=null?`<line x1="${L}" y1="${Y(target)}" x2="${W-R}" y2="${Y(target)}" stroke="#ff9f43" stroke-width="2" stroke-dasharray="7 6"/>`:'';return `<svg viewBox="0 0 ${W} ${H}" preserveAspectRatio="none" role="img">${grid}${goal}${line('theory','#20c77a')}${line('actual','#4b82ff')}${dots('theory','#20c77a')}${dots('actual','#4b82ff')}<text x="${L}" y="${H-8}">${data[0].dateShort||''}</text><text x="${W-R-70}" y="${H-8}">${data[data.length-1].dateShort||''}</text></svg>`}
window.charts=function(){const ds=chartDays(),s=S.settings;let cum=0;const rows=ds.map(d=>{const t=totals(d);cum+=t.balance;return{date:d,dateShort:new Date(d+'T12:00').toLocaleDateString(undefined,{day:'numeric',month:'short'}),deficit:Math.max(0,-cum),theory:s.startWeight+cum/Math.max(s.calPerKg,1),actual:S.days[d].weight==null?null:Number(S.days[d].weight)}});const deficitData=[{dateShort:'Start',theory:0,actual:null}];rows.forEach(r=>deficitData.push({dateShort:r.dateShort,theory:r.deficit,actual:null}));const latest=rows.filter(r=>r.actual!=null).at(-1),theoretical=rows.at(-1)?.theory??s.startWeight;return `<section class="card chartCard"><div class="title">Cumulative deficit</div><div class="chartSubtitle">Net progress from 0 toward ${fmtK(s.targetDeficit)} kcal</div><div class="bigChart">${makeChart(deficitData,s.targetDeficit,'kcal')}</div><div class="chartScale"><span>0 kcal</span><span>${fmtK(s.targetDeficit)} target</span></div></section><section class="card chartCard"><div class="title">Actual vs theoretical weight</div><div class="chartSubtitle">Green = theoretical from your logged calorie balance. Blue = your actual weigh-ins.</div><div class="legend"><span class="legendDot theoryDot"></span>Theoretical <span class="legendDot actualDot"></span>Actual <span class="legendDot goalDot"></span>Goal</div><div class="bigChart">${rows.length?makeChart(rows,s.goalWeight,'kg'):`<div class="chartEmpty"><b>Start logging</b><span>Your theoretical line will begin after your first tracked day.</span></div>`}</div><div class="weightCompare"><div><span class="label">THEORETICAL NOW</span><b>${theoretical.toFixed(1)} kg</b></div><div><span class="label">ACTUAL NOW</span><b>${latest?latest.actual.toFixed(1)+' kg':'—'}</b></div></div>${latest?`<div class="notice">Latest actual <b>${latest.actual.toFixed(1)} kg</b> · theoretical <b>${theoretical.toFixed(1)} kg</b> · difference <b>${(latest.actual-theoretical>=0?'+':'')+(latest.actual-theoretical).toFixed(1)} kg</b></div>`:'<div class="empty">Add your morning weight on Daily to put the actual line on this same chart.</div>'}</section>`};
const oldRender=window.render;window.render=function(){oldRender();if(page==='charts')document.querySelectorAll('.bigChart svg').forEach(svg=>svg.style.width='100%')};if(typeof render==='function')render();})();